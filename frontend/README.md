esto es facil y sencillo, para poder iniciar tanto backend como frontend solamente se necesitan hacer estos comandos:

npm install y npm start

esto teniendo en cuenta que esta instalado Node.js
segun los multiples testeos que he hecho no se necesita otra consideración.